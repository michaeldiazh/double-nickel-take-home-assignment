[
  {
    "testName": "building join clause for user to address with single relationship",
    "input": {
      "joinOperator": "join",
      "sourceTable": "user",
      "targetTable": "address",
      "relationship": [["address_id", "id"]]
    },
    "expected": "join address on user.address_id = address.id"
  },
  {
    "testName": "building left join clause for user to address",
    "input": {
      "joinOperator": "left join",
      "sourceTable": "user",
      "targetTable": "address",
      "relationship": [["address_id", "id"]]
    },
    "expected": "left join address on user.address_id = address.id"
  },
  {
    "testName": "building right join clause for user to address",
    "input": {
      "joinOperator": "right join",
      "sourceTable": "user",
      "targetTable": "address",
      "relationship": [["address_id", "id"]]
    },
    "expected": "right join address on user.address_id = address.id"
  },
  {
    "testName": "building join clause with multiple relationships",
    "input": {
      "joinOperator": "join",
      "sourceTable": "user",
      "targetTable": "address",
      "relationship": [
        ["address_id", "id"],
        ["secondary_address_id", "id"]
      ]
    },
    "expected": "join address on user.address_id = address.id and user.secondary_address_id = address.id"
  }
]

