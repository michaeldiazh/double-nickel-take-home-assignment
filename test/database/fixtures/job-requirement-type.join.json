[
  {
    "testName": "building join clause for job-requirements to job-requirement-type with single relationship",
    "input": {
      "joinOperator": "join",
      "sourceTable": "job_requirements",
      "targetTable": "job_requirement_type",
      "relationship": [["requirement_type_id", "id"]]
    },
    "expected": "join job_requirement_type on job_requirements.requirement_type_id = job_requirement_type.id"
  },
  {
    "testName": "building left join clause for job-requirements to job-requirement-type",
    "input": {
      "joinOperator": "left join",
      "sourceTable": "job_requirements",
      "targetTable": "job_requirement_type",
      "relationship": [["requirement_type_id", "id"]]
    },
    "expected": "left join job_requirement_type on job_requirements.requirement_type_id = job_requirement_type.id"
  }
]

