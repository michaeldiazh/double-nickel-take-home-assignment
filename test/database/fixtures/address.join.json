[
  {
    "testName": "building join clause for address to user with single relationship",
    "input": {
      "joinOperator": "join",
      "sourceTable": "address",
      "targetTable": "user",
      "relationship": [["id", "address_id"]]
    },
    "expected": "join user on address.id = user.address_id"
  },
  {
    "testName": "building left join clause for address to user",
    "input": {
      "joinOperator": "left join",
      "sourceTable": "address",
      "targetTable": "user",
      "relationship": [["id", "address_id"]]
    },
    "expected": "left join user on address.id = user.address_id"
  },
  {
    "testName": "building right join clause for address to user",
    "input": {
      "joinOperator": "right join",
      "sourceTable": "address",
      "targetTable": "user",
      "relationship": [["id", "address_id"]]
    },
    "expected": "right join user on address.id = user.address_id"
  },
  {
    "testName": "building join clause with composite key relationship",
    "input": {
      "joinOperator": "join",
      "sourceTable": "address",
      "targetTable": "user",
      "relationship": [
        ["id", "address_id"],
        ["state", "preferred_state"]
      ]
    },
    "expected": "join user on address.id = user.address_id and address.state = user.preferred_state"
  }
]

