[
  {
    "testName": "building where clause for address with single condition",
    "input": [
      {
        "tableName": "address",
        "column": "city",
        "filter": "=",
        "value": "New York"
      }
    ],
    "expected": {
      "clause": "where address.city = $1",
      "values": ["New York"]
    }
  },
  {
    "testName": "building where clause for address with multiple AND conditions",
    "input": [
      {
        "tableName": "address",
        "column": "city",
        "filter": "=",
        "value": "San Francisco"
      },
      {
        "tableName": "address",
        "column": "state",
        "filter": "=",
        "value": "CA"
      },
      {
        "tableName": "address",
        "column": "zip_code",
        "filter": "like",
        "value": "94%"
      }
    ],
    "expected": {
      "clause": "where address.city = $1 and address.state = $2 and address.zip_code like $3",
      "values": ["San Francisco", "CA", "94%"]
    }
  },
  {
    "testName": "building where clause for address with state IN operator",
    "input": [
      {
        "tableName": "address",
        "column": "state",
        "filter": "in",
        "value": ["NY", "CA", "TX"]
      }
    ],
    "expected": {
      "clause": "where address.state in ($1, $2, $3)",
      "values": ["NY", "CA", "TX"]
    }
  },
  {
    "testName": "building where clause for address with NOT IN operator",
    "input": [
      {
        "tableName": "address",
        "column": "state",
        "filter": "not in",
        "value": ["AK", "HI"]
      }
    ],
    "expected": {
      "clause": "where address.state not in ($1, $2)",
      "values": ["AK", "HI"]
    }
  },
  {
    "testName": "building where clause for address with IS NULL operator",
    "input": [
      {
        "tableName": "address",
        "column": "apt_number",
        "filter": "is null"
      }
    ],
    "expected": {
      "clause": "where address.apt_number is null",
      "values": []
    }
  },
  {
    "testName": "building where clause for address with IS NOT NULL operator",
    "input": [
      {
        "tableName": "address",
        "column": "apt_number",
        "filter": "is not null"
      }
    ],
    "expected": {
      "clause": "where address.apt_number is not null",
      "values": []
    }
  },
  {
    "testName": "building where clause for address with comparison operators",
    "input": [
      {
        "tableName": "address",
        "column": "created_at",
        "filter": ">=",
        "value": "2024-01-01T00:00:00.000Z"
      },
      {
        "tableName": "address",
        "column": "created_at",
        "filter": "<=",
        "value": "2024-12-31T00:00:00.000Z"
      }
    ],
    "expected": {
      "clause": "where address.created_at >= $1 and address.created_at <= $2",
      "values": ["2024-01-01T00:00:00.000Z", "2024-12-31T00:00:00.000Z"]
    }
  },
  {
    "testName": "building where clause for address with OR composition",
    "input": {
      "compositionOption": "or",
      "whereOptions": [
        {
          "tableName": "address",
          "column": "city",
          "filter": "=",
          "value": "New York"
        },
        {
          "tableName": "address",
          "column": "city",
          "filter": "=",
          "value": "Los Angeles"
        }
      ]
    },
    "expected": {
      "clause": "(address.city = $1 or address.city = $2)",
      "values": ["New York", "Los Angeles"]
    }
  },
  {
    "testName": "building where clause for address with complex AND composition",
    "input": {
      "compositionOption": "and",
      "whereOptions": [
        {
          "tableName": "address",
          "column": "state",
          "filter": "in",
          "value": ["CA", "NY"]
        },
        {
          "tableName": "address",
          "column": "zip_code",
          "filter": "like",
          "value": "9%"
        }
      ]
    },
    "expected": {
      "clause": "(address.state in ($1, $2) and address.zip_code like $3)",
      "values": ["CA", "NY", "9%"]
    }
  }
]

