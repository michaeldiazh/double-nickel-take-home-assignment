[
  {
    "testName": "building join clause for application to user with single relationship",
    "input": {
      "joinOperator": "join",
      "sourceTable": "application",
      "targetTable": "user",
      "relationship": [["user_id", "id"]]
    },
    "expected": "join user on application.user_id = user.id"
  },
  {
    "testName": "building left join clause for application to user",
    "input": {
      "joinOperator": "left join",
      "sourceTable": "application",
      "targetTable": "user",
      "relationship": [["user_id", "id"]]
    },
    "expected": "left join user on application.user_id = user.id"
  },
  {
    "testName": "building join clause for application to job",
    "input": {
      "joinOperator": "join",
      "sourceTable": "application",
      "targetTable": "job",
      "relationship": [["job_id", "id"]]
    },
    "expected": "join job on application.job_id = job.id"
  },
  {
    "testName": "building right join clause for application to job",
    "input": {
      "joinOperator": "right join",
      "sourceTable": "application",
      "targetTable": "job",
      "relationship": [["job_id", "id"]]
    },
    "expected": "right join job on application.job_id = job.id"
  }
]

